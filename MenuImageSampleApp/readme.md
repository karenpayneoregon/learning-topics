# About

An example for creating a console menu from image names from a database table, which, when selecting a menu item, saves the image to disk.

For the record, sure, the images could be in the file system, but here they are under 200x200, which I use for demonstrations.
