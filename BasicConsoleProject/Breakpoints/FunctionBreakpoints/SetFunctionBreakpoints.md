# Set function breakpoints

You can break execution when a `function` is called. This is useful, for example, when you know the function name but not its location. 

It is also useful if you have functions with the same name and you want to break on them all (such as overloaded functions or functions in different projects).

:book: [Microsoft documentation](https://learn.microsoft.com/en-us/visualstudio/debugger/using-breakpoints?view=visualstudio&pivots=programming-language-dotnet#BKMK_Set_a_breakpoint_in_a_source_file)